version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./:/app:delegated
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=8080
    command: npm run dev -- --host 0.0.0.0 --port 8080
